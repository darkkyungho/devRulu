<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar_url(:thumb), class: 'img-circle thumb' %>
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>